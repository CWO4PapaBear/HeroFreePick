Combat preparation preservation
===============================

Apply the reviewed payload to the cumulative PTR client. It extends the Martial Fluidity UI payload, retaining tooltip, primary-stat and lifecycle updates absent from the older base addon. Combat bypasses close-time primary-stat validation and hides pending-review/mode-choice overlays without applying or discarding the SavedVariables draft. Reopen after combat to resume. No server or database changes.
