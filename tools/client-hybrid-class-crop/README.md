# Circular class artwork and staggered picker rows

Incremental two-file overlay after client-hybrid-class-circles. Check both manifest before hashes before installing; reconcile newer clients instead of overwriting them. Back up the current files and keep saved variables/layout overrides.

The Hybrid picker and Hero Advancement class headers now crop the stock square icon border and render artwork clipped to a circle inside the existing ring. Static horizontal texture strips use supported 3.3.5 APIs; no modern texture-mask API or external image asset is required. The four lower choices are shifted half a column, placing each beneath the gap between the five above in both Hybrid selection routes.

Lua 5.1 syntax, ten class descriptions, localized fallback, callbacks and tooltip cleanup pass. Local installation verified with hashes/backups. In-game appearance remains pending review. No server changes, launcher assets or channel promotion. Root build.py does not incorporate this versioned overlay.
