# Hybrid policy module

Requires sibling `mod-hero-advancement` only. No other mode module is required. Place both folders under AzerothCore/modules, then rebuild. This release registers server-side rules only; learning, refunds, character-choice persistence and addon transport are not implemented. Do not deploy as a working free-pick progression system. No core source patch is supplied.
